query footer($limit: Int!, $preview: Boolean!) {
  footerCollection(limit: $limit, preview: $preview) {
    items {
      socialMediaLinksCollection {
        items {
          socialMediaSite
          url
        }
      }
      copyrightText {
        text
      }
      backgroundImage {
        url
      }
    }
  }
}
